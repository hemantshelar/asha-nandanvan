@page "/admin/orders"
@attribute [Authorize(Roles = AppRoles.Admin)]
@inject NavigationManager Navigation

<PageTitle>All orders — Admin</PageTitle>

@code {
    protected override void OnInitialized() => Navigation.NavigateTo("/orders");
}
